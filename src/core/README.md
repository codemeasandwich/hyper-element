# src/core/

Core state management and constants for hyper-element.

## Overview

This module provides:
- Global manager for tracking element instances and their state
- Shared attributes storage for passing functions/objects between parent/child elements
- Regex patterns for detecting custom element tags
